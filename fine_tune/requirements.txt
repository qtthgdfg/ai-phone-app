# fine_tune/requirements.txt
# Install with: pip install -r fine_tune/requirements.txt

torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
peft>=0.10.0          # LoRA
trl>=0.8.6            # SFTTrainer
bitsandbytes>=0.43.0  # 4-bit quantization (CUDA only)
accelerate>=0.29.0
sentencepiece>=0.2.0
huggingface_hub>=0.22.0
tensorboard>=2.16.0
